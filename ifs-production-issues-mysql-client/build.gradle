apply from: rootProject.file("gradle-support/openshift-build.gradle")

buildscript {
    repositories { jcenter() }
}

buildDocker.tag = "docker-ifs.devops.innovateuk.org/releases/ifs-production-issues-mysql-client:" + sqlVersion